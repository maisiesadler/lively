name: Use GitHub Action

on:
  push:
    branches: [ gh-action-test ]

jobs:

  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-dotnet@v1
      with:
          source-url: https://api.nuget.org/v3/index.json
      env:
          NUGET_AUTH_TOKEN: ${{secrets.NUGET_AUTH_TOKEN}}

    - name: Build project
      run: dotnet build -c Release

  generate_yumlmd:
    runs-on: ubuntu-latest
    needs: [build]
    
    steps:
    - name: Generate yumlmd
      id: generate
      uses: maisiesadler/deptree@v1.0.3-gh-action-test.0
      env:
        ASSEMBLY_LOCATION: './src/DepTree/bin/Release/net5.0/DepTree.dll'
        ROOT_TYPES: 'DepTree.DependencyTree'
        INTERFACE_RESOLVER: None

    - name: Output yumlmd
      shell: bash
      run: echo '${{ steps.generate.outputs.result }}' > example-outputs/yumlmd.md

  generate_yuml:
    runs-on: ubuntu-latest
    needs: [build]
    
    steps:
    - name: Generate yuml
      id: generate
      uses: maisiesadler/deptree@v1.0.3-gh-action-test.0
      env:
        ASSEMBLY_LOCATION: './src/DepTree/bin/Release/net5.0/DepTree.dll'
        ROOT_TYPES: 'DepTree.DependencyTree'
        INTERFACE_RESOLVER: None
        OUTPUT_FORMAT: yuml

    - name: Output yuml
      shell: bash
      run: echo '${{ steps.generate.outputs.result }}' > example-outputs/yuml.yuml

  commit:
    runs-on: ubuntu-latest
    needs: [generate_yumlmd, generate_yuml]
    
    steps:
    - uses: EndBug/add-and-commit@v7
      with:
        default_author: github_actions
        message: 'Generate diagrams'
